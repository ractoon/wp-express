{
  "private": true,
  "scripts": {
    "start": "./node_modules/.bin/electron ./app",
    "dev": "./node_modules/.bin/electron ./app --debug"
  },
  "devDependencies": {
    "electron": "^2.0.2",
    "rimraf": "^2.5.2"
  }
}
